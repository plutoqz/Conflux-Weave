# Conflux-Weave v0.3 P6 · 执行记录与 Gate 验收

- 日期：2026-09-12
- 基线：`9b62388`（599 passed）→ 完成后 `670 passed`
- 依据：`docs/plans/current/v0.3-P6-具体执行方案.md`
- 本文为 P6-A/B/C 全部 Gate 的实际执行记录（验收顺序按方案第 7 节）。

## Gate 1 · A1 成果导出 ✅（commit 见 git log）

- 实现：`export_bundle.ExportService`（只读收集）+ Markdown/BibTeX/JSON/ZIP 渲染器 + 4 条导出 API（run/evidence/chat answer/note）+ 前端导出按钮（Research Studio 四格式、对话回答、笔记 MD）。
- 关键决策：报告正文已内嵌引用清单（来源清单等标题）时不再追加导出版清单，保证"正文引用编号与引用列表一致"；本地来源标题/作者/年份/DOI 经检索分段表、文库注册表、corpus 清单三级回填；不导出 Provider key 与内部配置（契约测试断言）。
- 验收记录：
  - 无引用、无答案、partial 报告可导出（document_reading run 导出为纯正文，0 citations 合法）；中文/公式/表格 UTF-8 完整；重复导出除 exported_at 外字节稳定（测试覆盖）。
  - 真实服务验证：deep_research / managed_verified_research / document_reading 三类 Run 导出成功；BibTeX 携带真实论文标题、作者、年份、页码；ZIP 三成员完整；非法格式 422。
- 未覆盖边界：PDF/Word 高保真导出（P6-D 明示不纳入）。

## Gate 2 · A2 数据生命周期 ✅

- 实现：migration 9（runs.archived_at/deleted_at + document_lifecycle 表）+ LifecycleRepositoryMixin + 对话重命名/归档/软删除/恢复 API + Run 与文档 lifecycle 端点 + 前端三处筛选与操作（对话侧栏、Overview 运行列表、Library 卡片）。
- 验收记录：归档/删除对象不出现在默认列表（repository 与 API 双层测试）；恢复后原 ID、消息、Evidence 与 Run 关系不变；软删除 Run 的 Evidence 包仍可导出；重复删除/恢复幂等；页面刷新状态一致（状态在 SQLite 权威层）。
- 失败案例记录：降级夹具（人为回退 schema_migrations）需物理回滚 migration 变更，否则 ALTER 重放报 duplicate column——已修正三处测试并在后续 migration 复用该模式。

## Gate 3 · A3 长任务反馈与通知 ✅

- 实现：ProgressResponse 增加 current_phase（人类可读阶段标签；契约测试保证不泄露内部 step kind）/ last_event_message / last_event_at / updated_at；`get_latest_run_event` 仓库方法；前端 `subscribeRunEvents`（SSE + 游标追踪 + 断线 2s 重连，服务端从 run_events 表补发）；浏览器 Notification（点击聚焦并跳转报告/错误）；页面内完成横幅与阶段展示；activeRunId 持久化到 localStorage 实现刷新恢复。
- 验收记录：真实服务 SSE `?after=0` 对 9 月 7 日历史 Run 补发事件成功（持久化重放实证）；进度响应携带阶段与最近事件；SSE 断线重连不重复渲染关键事件（游标单调递增 + 终态去重）。
- 设计取舍：轮询保留为降级兜底（SSE 不可用时任务推进仍可见）。

## Gate 4 · A4 React 前端浏览器回归 ✅

- 实现：`scripts/verify_react_workbench.mjs`（Playwright + 系统 Edge，React 版冒烟：五分区加载、真实导出下载、生命周期筛选、刷新恢复、三档视口 1440/1280/390 无横向溢出、控制台错误记账）。
- 验收记录：`var/acceptance/v0.3-p6-a4/`（8 张截图 + summary.json = PASS）；真实下载 `run-41434d…-run_report.md` 断言通过；窄桌面/移动宽度无溢出。
- 发现并修复：Topbar 导航在 390px 视口横向溢出 150px → 导航可横向滚动 + 紧凑间距（`pnpm build` 后复测通过）。
- 已知既有数据问题（非本次回归引入）：`run-test-f3c7214c` 报告产物文件缺失 → 503/409；UI 已优雅降级（空态 + 完成横幅）；在验收 summary 中单列，不计入回归失败。

## Gate 5 · A5 设置生效反馈与 NotePatch ✅（2026-09-13 补验收）

- 实现：前端 Provider 保存由 POST 修复为 PUT（移除与 `extra="forbid"` 冲突的多余字段，修复前保存必然失败）；保存反馈三要素（成功/失败分色横幅、消费 `requires_restart` 显示"重启服务后生效"、`GET /api/v1/config` 新增 `provider_effective` 进程生效值面板）；设置页新增 Embedding/Reranker/Engine 模型字段与"测试连通性"按钮（chat+embedding 双探测，展示延迟/维度/token 记账）。
- NotePatch：前端修订字段由 `patch_prompt` 修复为契约的 `instruction`（修复前 UI 修订必然 422）；409 响应体新增 `latest_version`（契约测试），前端 409 → 基线刷新 → 自动重应用一次 → 再冲突转人工。
- 验收记录：`var/acceptance/v0.3-p6-a5/`（4 张截图：字段+生效面板、保存反馈、Note Studio v2 修订落库、双探针测试）；API 级 `test_config_api.py` 12 项 + 409 契约测试。
- 验收发现：现行不可变链式笔记设计下，UI 侧自然 409 不可达（同 id 版本恒定）；409 契约保留作 API 直调防御，前端重应用逻辑就绪。详见 `var/acceptance/v0.3-p6-a5/summary.md`。

## Gate 5 · B1 语义记忆召回 ✅（2026-09-13 真 embedding 补验完成）

- 实现：`memory_recall.MemoryRecallService`（结构化过滤 → 语义+词法候选 → recency/usefulness 重排 → 层级配额 5/5/3 + 总量上限）；独立 LanceDB 记忆向量索引（懒加载增量写入，进程内暴力余弦评分）；每条召回带 memory_id/scope/score/reason（语义匹配/词法匹配/近期更新/确定性兜底）。
- 注入链路：`format_prompt_context(query=...)` 召回驱动注入（预算 max_chars 不变）；chat 直答与 RAG 问答把用户问题作为召回查询。
- 恢复语义（方案验收逐条）：相关记忆优先于最新（stub embedder 测试）；跨项目零泄漏（scope/target_id 精确过滤先于打分）；无相关记忆不强行注入（语义路径可用时返回空）；召回系统不可用退回 recency 确定性路径且不抛错；注入总量不超 L0/L1 既定上限。
- 前端：Memory Studio 召回预览面板（输入查询 → 语句 + score + reason 标签）。
- **真实 embedding 验证（2026-09-13 补验，`var/acceptance/v0.3-p6-b1/`）**：冻结 12 条记忆 fixture + 零词法重叠配对查询矩阵。结果：配对语义命中 5/6（reason=语义匹配，score 0.47–0.57，门槛 ≥5）；无关查询零误注入；跨 scope 零泄漏；层级召回正常；清空 embedding 配置后降级路径回归通过。Embedding 探测：text-embedding-v4 可用（1024 维）、text-embedding-3-small 可用（1536 维）、embedding-3 中转站 503。
- **补验暴露并修复 2 个 P1 缺陷**：① create_app 接线 `getattr(pipeline, "embedding")` 在 P2.3 多模态包装层下取到 None，语义召回自多模态合入起静默断裂（回归测试覆盖）；② lancedb 0.37 `list_tables()` 返回分页结构致重启后表存在性误判，索引存在时语义召回永久降级且重复付费嵌入（改为先 open 再 create）。附带：未配置 embedding_model 时不接线 embedder（避免缺省模型静默付费调用）；召回端点新增 `index_error` 可观测字段。
- B1 状态：机制实现 ✅ / 离线语义排序 ✅ / **真实 embedding 语义召回 ✅（本轮补验）** / 无 Provider 降级 ✅（本轮真实验证）。
- 已知边界：语义门槛 0.35 下近义干扰记忆可压过目标（A3 案例，记录为边界非失效）；fixture 验证后已清理（12/12 删除）。

## Gate 6 · B2/B3 受限计算工具与权限分级 ✅

- 实现：`compute_tools.RestrictedComputeSandbox`——一次性临时工作区、input/ 只读挂载、output/ 白名单（子进程 cwd=output）、跨平台超时终止、POSIX RLIMIT_AS 内存上限（Windows 记录 "unavailable"）、stdout 截断标记、声明产物校验、自动清理；bootstrap 守卫封禁网络/进程模块（socket/ssl/http/urllib/subprocess/asyncio/ctypes…）并接管 open()（读写均限沙箱目录）；os.system/exec/popen 拒绝。
- B3：`ToolPolicy`（read-only/compute/external-write 三级 + 默认策略：前两者受限自动、external-write 必须 HITL 审批记录）+ allowed/denied 列表 + 决策顺序（禁止 > 允许列表 > 类默认 > 审批）。
- API：`POST /api/v1/tools/compute`（Tool Contract：script/input_artifacts/output_paths/timeout_seconds）；携带 run_id 时经 agent_events 记录 tool_call 与产物关系；沙箱错误全部折叠进结果对象（API 进程不退出）。
- 验收记录：15 项测试逐条覆盖方案验收（产物生成/超时终止/非法路径/网络拒绝/大输出限制/错误隔离/Run 可见）。

## Gate 7 · B4 预算硬限制 ✅

- 实现：migration 10（tool_budget_reservations/tool_budget_usage，脱离 attempt 耦合）+ `ToolBudgetMixin`（reserve→execute→settle/release 生命周期；超限抛 ToolBudgetExceeded；stop 后 budget_limits.state='stopped' 拒绝一切后续预留）；compute 端点集成预算门。
- 验收记录（方案逐条）：预算不足不发起执行（测试断言 stdout_artifact_id=None）；工具超限后不可经兜底绕过（重复调用仍 budget_exceeded）；失败结果记录 budget_exceeded；RUNNING 态 Run 转 failed（状态机允许范围内；非运行态以 budget.state=stopped 为权威标记）；BudgetResponse 暴露 limit/reserved/actual/remaining，ResearchView 预算行 + 已超限停止徽标。
- 失败案例记录：endpoint 内 transition FAILED 曾因 RunStatus 未在模块级导入被静默吞掉——补导入并新增断言防回归。

## Gate 8 · C1 API/Worker 进程分离 ✅

- 实现：`build_research_runtimes()`（无 HTTP 的权威运行时栈工厂，API/Worker 共用）+ `create_app(enable_worker=)` + `serve --no-worker` + 新命令 `conflux-weave worker`（独立进程、独立 worker 身份、可选监督时长）。
- 事件 outbox 语义：run_events 在状态事务内写入（W3 起既有），A3 的游标补发即 outbox 重放；未新建第二张 outbox 表（避免双写）。
- 验收记录（方案逐条，tests/test_process_split.py + 真实双进程）：
  - Worker 重启不影响 API：enable_worker=False 的 API 全功能，任务停在队列；
  - API 重启不丢待执行任务：重开应用后队列任务仍在并被执行；
  - 崩溃 lease 过期 + 他者接管：过期后 worker-b 认领同一步骤（契约测试）；
  - 事件可从数据库补发：跨连接游标重放一致；
  - 已完成步骤不重复执行：complete 后 claim 不再返回该步骤。
  - 真实验证：`serve --no-worker` + 独立 `worker` 进程完成一次 **fixture** 研究提交（API 提交 → worker 认领 → 交付 partial）。真实 Provider 深度研究全链路的双进程验证列入 C2 第二批收尾一并执行。

## Gate 9 · C2 Step 级 checkpoint 与恢复 ✅ 第一批（retrieve/deliver 外层闭环）

> 状态措辞（2026-09-13 评估后修正）：**C2 第一批已完成：retrieve/deliver 外层恢复闭环；完整目标未完成：plan/claim/verify/synthesize/write 内部阶段统一 checkpoint 尚未入账**，恢复粒度目前仍停留在"研究批次"边界，未达到完整 step-level durable execution。

- 实现：migration 11（step_checkpoints 台账：chain_phase plan…deliver、input_digest、output_artifact、attempt、status、error_ref、起止时间、resumable）+ `StepCheckpointMixin`（record/list/reuse/unknown_outcome）+ DurableResearchRuntime 集成：
  - retrieve 阶段成功后记录 checkpoint；重试时同输入摘要直接复用 EXECUTION 产物完成 attempt——不重新授权外部调用、零新增预算消耗、报告产物不变（"不从头重跑"）；
  - deliver 阶段发布后记录 checkpoint（resumable=0，交付即终态）；
  - 可重放只读步骤失败自动重试（≤3 attempt，`requeue_failed_step`），付费/外部副作用禁止自动重试（维持 W3 语义）；不确定结果标记 unknown_outcome；
  - 原始失败记录全部保留（errors 表不清理）。
- 验收记录：5 项测试覆盖"worker 重启后从 checkpoint 继续 / 批次仅执行一次 / 失败记录保留 / unknown_outcome 不可复用 / 台账 API 可追溯每一步"。
- 边界声明：粒度按方案第一批为深度研究主链路的 retrieve/deliver 外层步骤 + 台账化 inner checkpoint 产物；plan/claim/verify/synthesize 等内部阶段沿用 W3 既有检查点产物（search/rank/synthesis-checkpoint），尚未逐段入账。

## 收尾状态（2026-09-13 完善轮后更新）

- 全量回归：**677 passed**（基线 599 → P6 首轮 670 → 完善轮 +7：A5/B1 端点与接线回归）。
- 生产服务：`conflux-weave serve` 已多次分离式重启验证（含接线修复与 lancedb 表判定修复后），health/ready + 导出/召回/设置保存/连通性探测冒烟通过；真实语义召回重启后可用（`semantic_available=true`）。
- 完善轮新增 Gate：A5（设置反馈 + NotePatch，见上）；B1 真 embedding 补验（见 Gate 5）。
- P6-D（鉴权多租户/备份 PITR/OTel/压测/灰度/Skill 市场/PDF 高保真导出）按方案维持暂缓，未纳入本轮。
- 遗留风险登记（评估后更新）：
  1. `run-test-f3c7214c` 报告产物文件缺失（既有数据损伤，UI 优雅降级）；
  2. compute 沙箱 bootstrap 守卫属单机研究工具级防护（非对抗性隔离；Windows 内存上限显示 unavailable）；
  3. C2 内部阶段（plan/claim/verify/synthesize/write）台账化待后续批次（第二批设计：phase-observer 回调入账 + 逐阶段 input_digest + 边界故障注入测试 + 真实 Provider 双进程 kill/resume 演练，预估 2–3 天）；
  4. 真实 Provider 深度研究全链路的双进程验证随 C2 第二批执行；
  5. A4 浏览器覆盖仅 Edge（Firefox/WebKit/原生读屏待扩展批次）；
  6. A5 NotePatch 自然 409 在不可变链式笔记设计下不可达（契约保留为 API 直调防御）。
