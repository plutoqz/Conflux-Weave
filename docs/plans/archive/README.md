# Legacy archive location

Plans retired with a design generation now live under versioned
`docs/plans/deprecated/` directories. The v0.2 W0-W5 plans and acceptance records
are in `../deprecated/v0.2/`.

This directory remains only to avoid silently reusing the old unversioned archive
convention. Do not place new documents here.
