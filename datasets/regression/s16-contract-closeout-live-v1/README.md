# S1.6 affected-surface contract closeout

该协议只验证 S1.6-C 暴露且由 S1.6-D 修改的三个结构合同：一个 discovery verifier
和两个 Manager plan/coverage 流程。执行前必须通过使用相同生产 Prompt 和 parser 的
schema canary。

它不重复 single-Agent、no-answer、UI 或 retrieval 验收，不修改任何 S1.6-C Run，
也不以通过结果声称 Manager 质量、成本或延迟收益。Provider 失败不自动重试；失败本身
进入新命名空间的不可变证据。
